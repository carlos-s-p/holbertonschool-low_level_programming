#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints the alphabet reverse
 *
 * Return: Returns 0
 */
int main(void)
{
	int n = 122;

	while ((n >= 97))
	{
	putchar(n);
	n--;
	}
	putchar(13);
	return (0);
}

